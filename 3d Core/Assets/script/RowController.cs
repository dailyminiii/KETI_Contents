using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RowController : MonoBehaviour
{
    [SerializeField]
    float interval; // 개체 간 간격 사이

    [SerializeField]
    int allObjNum; // 전체 객체 개수
    [SerializeField]
    int eachSprite; // 개체별 스프라이트
    [SerializeField]
    int showObjNum; // 보여줄 오브젝트 개수

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame

}
